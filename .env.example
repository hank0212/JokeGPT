# Optional: Comet ML experiment tracking.
# Copy to .env and fill in, then run training with --logger comet.
# Never commit the filled-in .env.
COMET_API_KEY=
COMET_PROJECT_NAME=JokeGPT
COMET_WORKSPACE=
